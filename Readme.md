## This is a simple example repository on Cucumber DataTables

### Usage
1. Clone this repo and extract it
2. Execute the command `npm install` to install the depedencies
3. To run this tests execute the command `npm test`
4. To generate the report execute command `npm run report` 